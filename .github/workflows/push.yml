name: HyperHDR Multi-Platform Builder

on:
  push:

jobs:
  linux_builds:
    name: Linux Builds
    runs-on: ubuntu-22.04
    permissions:
      packages: write
      contents: read
    strategy:
      matrix:
        include:
          # Debian/Ubuntu
          - distroName: debian
            distroVersion: bullseye
            architecture: amd64
          - distroName: debian
            distroVersion: bullseye
            architecture: arm64
          - distroName: debian
            distroVersion: bullseye
            architecture: armhf
          - distroName: debian
            distroVersion: trixie
            architecture: arm64
          - distroName: debian
            distroVersion: trixie
            architecture: armhf
          - distroName: ubuntu
            distroVersion: jammy
            architecture: amd64
          - distroName: ubuntu
            distroVersion: noble
            architecture: amd64
          # Archlinux
          - distroName: archlinux
            distroVersion: latest
            architecture: amd64
          # Fedora
          - distroName: fedora
            distroVersion: 43
            architecture: amd64

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU for ARM
        uses: docker/setup-qemu-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Configure Git for private repo
        run: |
          git config --global url."https://${{ secrets.GHCR_TOKEN }}:@github.com/".insteadOf "https://github.com/"

      - name: Show build info
        run: echo "Building for ${{ matrix.architecture }}/${{ matrix.distroName }}:${{ matrix.distroVersion }}"

      - name: Prepare Docker source
        shell: bash
        run: |
          # ARM vs AMD
          if [[ "${{ matrix.architecture }}" == "arm64" ]] || [[ "${{ matrix.architecture }}" == "armhf" ]]; then
              FINAL_DOCKER_SOURCE=balenalib/raspberrypi3-64:${{ matrix.distroVersion }}
          else
              FINAL_DOCKER_SOURCE=${{ matrix.distroName }}:${{ matrix.distroVersion }}
          fi

          # Determine build script
          if [[ "${{ matrix.distroName }}" == "fedora" ]]; then
              DOCKER_TOOL=buildFedora
          elif [[ "${{ matrix.distroName }}" == "archlinux" ]]; then
              DOCKER_TOOL=buildArch
          else
              DOCKER_TOOL=buildDebian
          fi

          echo "FINAL_DOCKER_SOURCE=$FINAL_DOCKER_SOURCE" >> $GITHUB_ENV
          echo "DOCKER_TOOL=$DOCKER_TOOL" >> $GITHUB_ENV

      - name: Make build scripts executable
        run: chmod +x buildDebian buildFedora buildArch

      - name: Build & push docker container
        env:
          PIXY_DOCKER_TAG: ghcr.io/pixylights-dev/${{ matrix.architecture }}/${{ matrix.distroName }}:${{ matrix.distroVersion }}
        run: |
          docker build \
            --pull \
            -t ${{ env.PIXY_DOCKER_TAG }} \
            --platform linux/${{ matrix.architecture }} \
            --build-arg DOCKER_SOURCE="${{ env.FINAL_DOCKER_SOURCE }}" \
            -f ${{ env.DOCKER_TOOL }} .

          docker push ${{ env.PIXY_DOCKER_TAG }}

  mac_builds:
    name: macOS Builds
    runs-on: macos-14
    strategy:
      matrix:
        architecture: [x86_64, arm64]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Show build info
        run: echo "Building HyperHDR macOS installer for ${{ matrix.architecture }}"

      - name: Build macOS installer
        shell: bash
        run: |
          ARCH="${{ matrix.architecture }}"
          ./buildMac.sh "$ARCH"
